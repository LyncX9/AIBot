services:
  - type: web
    name: nova-ai-bot
    env: python
    buildCommand: "pip install --upgrade pip && pip install --no-cache-dir --prefer-binary -r requirements.txt"
    startCommand: "python main.py"
    pythonVersion: 3.11.9
    envVars:
      - key: DISCORD_NO_AUDIO
        value: "1"
